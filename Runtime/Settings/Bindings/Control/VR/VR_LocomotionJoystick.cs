using UnityEngine;

namespace KadenZombie8.BIMOS
{
    public class VR_LocomotionJoystick : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
